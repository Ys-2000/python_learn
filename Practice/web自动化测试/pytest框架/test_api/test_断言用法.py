def test_dy():
    assert 1 == 1
    assert 1 != 2
    assert 1 < 2
    assert 2 > 1
    assert 'a' in 'abc'         # 断言'a'属于'abc'               # in 属于
    assert 'a' not in 'bcd'     # 断言'a'不属于'bcd'             # not in 不属于
    assert True is True         # 断言True是True                # is 是
    assert False is not True    # 断言False不是True             # is not 不是

