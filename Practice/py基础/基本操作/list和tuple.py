# list列表{}

listb = ["ale",126,True]
# listb.clear()     # clear()清空列表中的所有元素
# print(listb)
# lista = ["acs","cds",listb]
#
# # 在下标为1的位置增加
# lista.insert(1, "VBGN")
# # 在末尾增加
# lista.append("NIGg")
# # 删除第一个，pop()则删除最后一个
# lista.pop(0)
# # 替换最后一个下标
# lista[-1] = "MMM"
# print('输出列表：',lista[2][0],'长度为',len(lista), )
#
# print(lista,"\n",listb)



# tuple元组()   tuple不可变，所以代码更安全。如果可能，能用tuple代替list就尽量用tuple
# 只有1个元素的tuple定义时必须加一个逗号,，来消除歧义
# listb[1] = 152
# tuplea = (1,"adssa", listb)
# print(tuplea)


# a = ['c', 'b', 'a']
# # sort()方法可以用来对列表进行排序，默认是按照元素的字典顺序进行升序排序。字典顺序是根据字符的ASCII码值来进行排序的。
# a.sort()
# print(a)
#
# b = 'ale'
# x = b.replace('a','B')      # 用于将字符串中的指定子字符串替换为新的字符串
# print(x)